<%= form_for([@deck, @card]) do |f| %>
  <div>
    <%= f.label :front %>
    <%= f.text_field :front %>

    <%= f.label :back %>
    <%= f.text_field :back %>
  </div>
  <%= f.submit "Create card" %>
<% end %>
